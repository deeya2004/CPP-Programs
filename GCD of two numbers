#include <iostream>
using namespace std;

int main() {
  //program to find the gcd
  int a, b, gcd = 1;
  cout<<"Enter two numbers:";
  cin>>a>>b;
  for (int i = 1; i <= a || i <= b; i++) {
    if (a % i == 0 && b % i == 0)
      gcd = i;
  }

  cout << "The GCD is " << gcd;

  return 0;
};
